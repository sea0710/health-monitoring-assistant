# 指标监测项目设计方案评估

## 一、项目概述

| 项目属性 | 内容 |
|---------|------|
| 项目名称 | 愈见・血常规监测助手 |
| 目标平台 | 微信小程序（预留 H5/PC 扩展） |
| 使用范围 | 内部小范围测试，不对外公线上线 |
| 核心技术 | AI OCR 识别 + 微信小程序 |

### ✅ 已确认技术选型

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端框架 | 微信小程序原生 | 性能最优，符合需求 |
| 后端框架 | Node.js + Express | 生态成熟，与前端技术栈统一 |
| 数据库 | MySQL 8.0 | 关系型数据适合业务场景 |
| 部署方式 | Serverless（优先） | 后续可平滑迁移到云服务器 |
| 对象存储 | 待定（阿里云 OSS/腾讯云 COS） | 发布体验版前确认 |
| 图表库 | ECharts for 微信小程序 | 已有实现可复用 |
| 提醒功能 | 小程序订阅消息 | 用户已确认 |
| 微信 AppID | 发布体验版前提供 | 用户已确认 |

## 二、现有代码分析

### 已有项目结构（飞书妙答指标监测）

```
飞书妙答指标监测/
├── client/                    # 前端代码（React 19）
│   └── src/
│       ├── pages/             # 8个核心页面（已完成UI设计）
│       │   ├── LoginPage/         ✅ 登录/注册页
│       │   ├── HomePage/          ✅ 病历夹列表页
│       │   ├── PatientCreatePage/ ✅ 患者档案创建页
│       │   ├── ReportUploadPage/  ✅ 报告上传/OCR识别页
│       │   ├── ReportDetailPage/  ✅ 报告详情页
│       │   ├── TrendsPage/        ✅ 指标趋势图页
│       │   ├── GradeReferencePage/✅ 分级速查页
│       │   └── ReminderSettingsPage/✅ 提醒设置页
│       └── components/        # UI组件库（Radix UI + Tailwind）
├── server/                    # 后端代码（NestJS）
│   ├── capabilities/          # OCR 能力配置（AI图片转JSON）
│   └── modules/               # 业务模块
└── shared/                    # 前后端共享类型
```

### 现有技术栈 vs 目标技术栈

| 层级 | 现有技术栈 | 目标技术栈 | 迁移策略 |
|------|-----------|-----------|---------|
| 前端框架 | React 19 + TypeScript | 微信小程序原生 | **重写**（UI设计可复用） |
| 后端框架 | NestJS | Express | **迁移**（业务逻辑可复用） |
| ORM | Drizzle ORM | mysql2 / Sequelize | **迁移** |
| 数据库 | PostgreSQL | MySQL | **迁移**（SQL语法基本兼容） |
| 部署 | 飞书 AaaS 平台 | Serverless | **迁移** |

### 已完成的前端页面评估

| 页面 | 完成度 | UI质量 | 迁移工作量 |
|------|--------|--------|-----------|
| 登录/注册页 | 90% | 高 | 中等（需适配小程序API） |
| 病历夹列表页 | 95% | 高 | 中等 |
| 患者档案创建页 | 80% | 高 | 中等 |
| 报告上传/OCR识别页 | 90% | 高 | 较高（需适配小程序上传API） |
| 报告详情页 | 95% | 高 | 中等 |
| 指标趋势图页 | 90% | 高 | 较高（ECharts小程序适配） |
| 分级速查页 | 95% | 高 | 低 |
| 提醒设置页 | 80% | 高 | 中等（需适配订阅消息） |

### 可复用的核心资产

| 资产类型 | 内容 | 复用方式 |
|---------|------|---------|
| UI设计 | 8个页面完整设计 | 直接参考，转换为小程序WXML/WXSS |
| 业务逻辑 | OCR识别、分级计算、趋势分析 | 迁移到Express后端 |
| 数据结构 | 患者信息、报告数据、指标数据 | 转换为MySQL表结构 |
| OCR配置 | AI图片转JSON能力配置 | 迁移到新后端 |
| 分级数据 | WHO骨髓抑制分级标准 | 直接复用 |

## 三、核心功能模块

### P0 优先级（核心功能）

| 模块 | 功能描述 | 前端页面 | 后端API |
|------|---------|---------|---------|
| 登录/注册 | 手机号+密码认证，JWT token | login | POST /api/auth/login, POST /api/auth/register |
| 患者档案 | 姓名、化疗周期、化疗结束时间 | patient-create | GET/POST /api/patients |
| 报告上传/OCR | 图片上传、AI识别、手动编辑 | report-upload | POST /api/ocr, POST /api/reports |
| 异常解读 | WHO骨髓抑制分级、居家建议 | report-detail | GET /api/reports/:id, GET /api/interpretations/:id |
| 指标趋势图 | ECharts折线图、历史趋势 | trends | GET /api/trends/:patientId |

### P1 优先级（辅助功能）

| 模块 | 功能描述 | 前端页面 | 后端API |
|------|---------|---------|---------|
| 分级速查 | WHO分级标准速查表 | grade-reference | GET /api/references |
| 监测提醒 | 小程序订阅消息提醒 | reminder-settings | GET/POST /api/reminders |

### 六项核心监测指标

| 指标代码 | 指标名称 | 参考范围 | 单位 |
|---------|---------|---------|------|
| WBC | 白细胞计数 | 4.0-10.0 | ×10⁹/L |
| NEUT# | 中性粒细胞绝对值 | 2.0-7.0 | ×10⁹/L |
| NEUT% | 中性粒细胞百分比 | 50-70 | % |
| RBC | 红细胞计数 | 3.5-5.5 | ×10¹²/L |
| HGB | 血红蛋白 | 110-160 | g/L |
| PLT | 血小板计数 | 100-300 | ×10⁹/L |

## 四、数据库设计

### 数据表结构（6张核心表）

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│    user     │────<│   patient   │────<│   report    │
│  (用户表)    │     │  (患者表)    │     │  (报告表)    │
└─────────────┘     └─────────────┘     └─────────────┘
                           │                   │
                           │                   │
                    ┌──────┴──────┐     ┌──────┴──────┐
                    │  reminder   │     │  indicator  │
                    │ (提醒设置表) │     │  (指标表)    │
                    └─────────────┘     └─────────────┘
                                            
                    ┌─────────────┐
                    │  reference  │
                    │ (分级速查表) │
                    └─────────────┘
```

### MySQL 表结构定义

```sql
-- 用户表
CREATE TABLE user (
  user_id VARCHAR(64) PRIMARY KEY,
  phone VARCHAR(11) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL COMMENT 'BCrypt加密',
  nickname VARCHAR(50),
  avatar VARCHAR(255),
  wechat_openid VARCHAR(64),
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  is_deleted TINYINT DEFAULT 0
);

-- 患者表
CREATE TABLE patient (
  patient_id VARCHAR(64) PRIMARY KEY,
  user_id VARCHAR(64) NOT NULL,
  name VARCHAR(50) NOT NULL,
  gender VARCHAR(10),
  birthday DATE,
  tumor_type VARCHAR(50),
  treatment_plan VARCHAR(100),
  chemotherapy_cycles INT,
  last_chemo_end_date DATE,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  is_deleted TINYINT DEFAULT 0,
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);

-- 报告表
CREATE TABLE report (
  report_id VARCHAR(64) PRIMARY KEY,
  patient_id VARCHAR(64) NOT NULL,
  report_type VARCHAR(20) DEFAULT '血常规',
  test_time DATETIME NOT NULL,
  test_hospital VARCHAR(100),
  raw_image_url VARCHAR(255),
  is_edited TINYINT DEFAULT 0,
  source VARCHAR(20),
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  is_deleted TINYINT DEFAULT 0,
  FOREIGN KEY (patient_id) REFERENCES patient(patient_id),
  INDEX idx_test_time (test_time)
);

-- 指标表
CREATE TABLE indicator (
  indicator_id VARCHAR(64) PRIMARY KEY,
  report_id VARCHAR(64) NOT NULL,
  indicator_code VARCHAR(20) NOT NULL,
  indicator_name VARCHAR(50),
  test_value DECIMAL(10,2),
  reference_min DECIMAL(10,2),
  reference_max DECIMAL(10,2),
  unit VARCHAR(20),
  is_abnormal TINYINT DEFAULT 0,
  abnormal_level VARCHAR(10) COMMENT 'Ⅰ/Ⅱ/Ⅲ/Ⅳ',
  trend VARCHAR(20),
  remark VARCHAR(255),
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (report_id) REFERENCES report(report_id),
  INDEX idx_indicator_code (indicator_code)
);

-- 提醒设置表
CREATE TABLE reminder (
  reminder_id VARCHAR(64) PRIMARY KEY,
  patient_id VARCHAR(64) NOT NULL,
  reminder_date DATE NOT NULL,
  reminder_time TIME NOT NULL,
  is_enabled TINYINT DEFAULT 1,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  is_deleted TINYINT DEFAULT 0,
  FOREIGN KEY (patient_id) REFERENCES patient(patient_id)
);

-- 分级速查表
CREATE TABLE reference (
  reference_id VARCHAR(64) PRIMARY KEY,
  indicator_code VARCHAR(20) NOT NULL,
  indicator_name VARCHAR(50),
  grade_level VARCHAR(10) COMMENT 'Ⅰ/Ⅱ/Ⅲ/Ⅳ',
  grade_range VARCHAR(100),
  suggestion TEXT,
  note TEXT,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_indicator_code (indicator_code)
);
```

## 五、API 接口设计

### 认证模块

| 方法 | 路径 | 描述 | 请求体 | 响应 |
|------|------|------|--------|------|
| POST | /api/auth/register | 用户注册 | {phone, password} | {token, user} |
| POST | /api/auth/login | 用户登录 | {phone, password} | {token, user} |

### 患者模块

| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| GET | /api/patients/:userId | 获取患者档案 | JWT |
| POST | /api/patients | 创建/更新患者档案 | JWT |

### 报告模块

| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| GET | /api/reports/:patientId | 获取报告列表 | JWT |
| GET | /api/reports/detail/:id | 获取报告详情 | JWT |
| POST | /api/reports | 创建报告 | JWT |

### OCR 模块

| 方法 | 路径 | 描述 | 请求体 | 响应 |
|------|------|------|--------|------|
| POST | /api/ocr | OCR识别 | multipart/form-data (image) | {hospital, test_time, indicators...} |

### 趋势模块

| 方法 | 路径 | 描述 | 响应 |
|------|------|------|------|
| GET | /api/trends/:patientId | 获取趋势数据 | [{date, WBC, NEUT#, ...}] |

### 速查模块

| 方法 | 路径 | 描述 | 响应 |
|------|------|------|------|
| GET | /api/references | 获取分级速查数据 | [{code, name, grades...}] |

### 提醒模块

| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| GET | /api/reminders/:patientId | 获取提醒列表 | JWT |
| POST | /api/reminders | 创建提醒 | JWT |

## 六、项目目录结构

### 小程序前端

```
miniprogram/
├── pages/
│   ├── login/              # 登录/注册页
│   │   ├── login.wxml
│   │   ├── login.wxss
│   │   ├── login.js
│   │   └── login.json
│   ├── home/               # 病历夹列表页
│   ├── patient-create/     # 患者档案创建页
│   ├── report-upload/      # 报告上传页
│   ├── report-detail/      # 报告详情页
│   ├── trends/             # 指标趋势图页
│   ├── reference/          # 分级速查页
│   └── reminder/           # 提醒设置页
├── components/
│   ├── disclaimer/         # 免责声明组件
│   ├── navbar/             # 顶部导航栏
│   ├── indicator-card/     # 指标卡片
│   └── trend-chart/        # 趋势图表
├── utils/
│   ├── api.js              # API封装
│   ├── auth.js             # 认证工具
│   ├── util.js             # 通用工具
│   └── constants.js        # 常量定义
├── images/                 # 图片资源
├── app.js                  # 小程序入口
├── app.json                # 小程序配置
├── app.wxss                # 全局样式
└── project.config.json     # 项目配置
```

### 后端服务

```
server/
├── src/
│   ├── routes/             # API路由
│   │   ├── auth.js         # 认证路由
│   │   ├── patients.js     # 患者路由
│   │   ├── reports.js      # 报告路由
│   │   ├── ocr.js          # OCR路由
│   │   ├── trends.js       # 趋势路由
│   │   ├── references.js   # 速查路由
│   │   └── reminders.js    # 提醒路由
│   ├── models/             # 数据模型
│   │   ├── user.js
│   │   ├── patient.js
│   │   ├── report.js
│   │   ├── indicator.js
│   │   ├── reminder.js
│   │   └── reference.js
│   ├── middleware/         # 中间件
│   │   ├── auth.js         # JWT鉴权
│   │   ├── errorHandler.js # 错误处理
│   │   └── validator.js    # 参数校验
│   ├── services/           # 业务逻辑
│   │   ├── ocr.js          # OCR服务
│   │   ├── grade.js        # 分级计算
│   │   └── wechat.js       # 微信服务
│   ├── config/             # 配置
│   │   ├── database.js     # 数据库配置
│   │   ├── jwt.js          # JWT配置
│   │   └── ocr.js          # OCR配置
│   └── app.js              # 应用入口
├── sql/
│   └── init.sql            # 数据库初始化脚本
├── package.json
└── serverless.yml          # Serverless配置
```

## 七、实施计划

### 第一阶段：项目初始化（1天）

- [ ] 创建微信小程序项目
- [ ] 创建Express后端项目
- [ ] 配置MySQL数据库
- [ ] 编写数据库初始化脚本
- [ ] 配置Serverless部署

### 第二阶段：后端核心开发（3-4天）

- [ ] 实现用户认证模块（注册、登录、JWT）
- [ ] 实现患者管理模块
- [ ] 实现报告管理模块
- [ ] 实现OCR识别模块（迁移现有AI能力）
- [ ] 实现趋势数据模块
- [ ] 实现分级速查模块
- [ ] 实现提醒设置模块

### 第三阶段：前端开发（4-5天）

- [ ] 实现登录/注册页
- [ ] 实现病历夹列表页
- [ ] 实现患者档案创建页
- [ ] 实现报告上传/OCR识别页
- [ ] 实现报告详情页
- [ ] 实现指标趋势图页（ECharts适配）
- [ ] 实现分级速查页
- [ ] 实现提醒设置页

### 第四阶段：联调测试（2天）

- [ ] 前后端联调
- [ ] OCR识别测试
- [ ] 小程序真机测试
- [ ] 性能优化

### 第五阶段：发布上线（1天）

- [ ] 配置微信小程序AppID
- [ ] 构建小程序代码包
- [ ] 上传微信公众平台
- [ ] 发布体验版

## 八、风险评估

| 风险项 | 风险等级 | 应对措施 |
|--------|---------|---------|
| OCR识别准确率不足 | 中 | 提供手动编辑功能，逐步优化识别规则 |
| 小程序审核被拒 | 中 | 严格遵循合规要求，选择「工具-健康工具」类目 |
| Serverless冷启动延迟 | 低 | 使用预留实例，优化代码体积 |
| 数据安全合规 | 高 | 密码BCrypt加密、JWT鉴权、免责声明 |

## 九、合规要点

### 必须遵守

1. **免责声明**：所有涉及解读的页面必须固定展示免责声明
2. **文案规范**：避免使用「诊疗」「治疗建议」「处方」等词汇
3. **类目选择**：选择「工具 - 健康工具」，不要选医疗类目
4. **数据安全**：用户密码BCrypt加密存储，所有API校验JWT token

### 免责声明文案

> 本工具仅供内部测试与健康科普参考，不构成任何诊疗建议，不能替代执业医师的专业判断

---

**方案已完成，请确认后开始实施。**
