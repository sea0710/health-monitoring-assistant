<view class="container">
  <view class="logo-section">
    <view class="logo-icon">
      <image src="/images/logo.svg" class="logo-image" mode="aspectFit"></image>
    </view>
    <text class="app-title">愈见</text>
    <text class="app-subtitle">血常规监测助手</text>
  </view>

  <view class="form-section">
    <view class="input-group">
      <text class="input-label">手机号</text>
      <input 
        class="input-field" 
        type="number" 
        placeholder="请输入11位手机号" 
        maxlength="11"
        value="{{phone}}"
        bindinput="onPhoneInput"
      />
    </view>

    <view class="input-group mt-3">
      <text class="input-label">密码</text>
      <view class="password-input-wrapper">
        <input 
          class="input-field password-input" 
          type="{{showPassword ? 'text' : 'password'}}" 
          placeholder="请输入密码（至少6位）"
          value="{{password}}"
          bindinput="onPasswordInput"
        />
        <view class="password-toggle" bindtap="togglePassword">
          <image 
            src="{{showPassword ? '/images/eye-off.svg' : '/images/eye.svg'}}" 
            class="toggle-icon"
          ></image>
        </view>
      </view>
    </view>

    <button class="btn-primary btn-login" bindtap="handleLogin" loading="{{isLoading}}">
      {{isLoading ? '请稍候...' : '登录 / 注册'}}
    </button>

    <view class="agreement-text">
      <text>登录即表示您同意相关服务条款</text>
    </view>
  </view>

  <view class="disclaimer-section">
    <text class="disclaimer-text">{{disclaimerText}}</text>
  </view>
</view>
